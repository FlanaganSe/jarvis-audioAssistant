# Required — server crashes without these
OPENAI_API_KEY=sk-...
DATABASE_URL=postgres://user:password@localhost:5432/jarvis
GITHUB_TOKEN=github_pat_...
OPENWEATHERMAP_API_KEY=...

# Required in production — auto-generated in dev if missing
JWT_SECRET=

# Optional — defaults to redis://localhost:6379
REDIS_URL=redis://localhost:6379

# Optional — defaults to 3001 (Railway auto-injects this)
# PORT=3001
